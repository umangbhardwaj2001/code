#include <bits/stdc++.h>
using namespace std;
int main()
{
    stack<int> s;
    // cout << s.size();
    // s.push(30);
    // cout << s.size() << endl;
    // s.pop();
    cout << s.top();
    return 0;
}